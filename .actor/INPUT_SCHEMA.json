{
    "title": "Web Crawler",
    "description": "Simple web crawler with configurable request limits.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "maxRequestsPerCrawl": {
            "title": "✅ Max Requests Per Crawl (REQUIRED)",
            "type": "integer",
            "description": "Maximum number of requests to process - This field is required, use -1 for unlimited crawling",
            "minimum": 1,
            "maximum": 10000,
            "editor": "number",
            "prefill": 200
        }
    },
    "required": [
        "maxRequestsPerCrawl"
    ]
}