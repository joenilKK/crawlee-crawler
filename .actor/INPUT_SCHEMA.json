{
    "title": "Web Crawler",
    "description": "Simple web crawler with configurable output filename.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "outputFilename": {
            "title": "✅ Output Filename (REQUIRED)",
            "type": "string",
            "description": "Name of the output file (without .json extension) - This field is required",
            "minLength": 1,
            "maxLength": 100,
            "editor": "textfield",
            "prefill": "memc-specialists"
        }
    },
    "required": [
        "outputFilename"
    ]
}