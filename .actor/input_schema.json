{
  "title": "Data.gov.sg Crawler Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "resourceIds": {
      "title": "Resource IDs",
      "type": "array",
      "description": "Array of resource IDs to crawl from data.gov.sg",
      "editor": "stringList",
      "items": {
        "type": "string"
      }
    },
    "startDate": {
      "title": "Start Date",
      "type": "string",
      "description": "Start date for data crawling (YYYY-MM-DD format). Must be a valid date and not after End Date.",
      "editor": "date",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "minLength": 10,
      "maxLength": 10
    },
    "endDate": {
      "title": "End Date", 
      "type": "string",
      "description": "End date for data crawling (YYYY-MM-DD format). Must be a valid date and not before Start Date.",
      "editor": "date",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "minLength": 10,
      "maxLength": 10
    }
  },
  "required": ["resourceIds", "startDate", "endDate"]
}
