{
  "title": "Data.gov.sg Crawler Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "resourceMap": {
      "title": "Resource IDs",
      "type": "object",
      "description": "Key-value mapping of resource IDs to crawl from data.gov.sg",
      "additionalProperties": {
        "type": "string"
      }
    },
    "startDate": {
      "title": "Start Date",
      "type": "string",
      "description": "Start date for data crawling (YYYY-MM-DD format). Must be a valid date and not after End Date.",
      "editor": "textfield",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "minLength": 10,
      "maxLength": 10
    },
    "endDate": {
      "title": "End Date", 
      "type": "string",
      "description": "End date for data crawling (YYYY-MM-DD format). Must be a valid date and not before Start Date.",
      "editor": "textfield",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "minLength": 10,
      "maxLength": 10
    }
  },
  "required": ["resourceMap", "startDate", "endDate"]
}
